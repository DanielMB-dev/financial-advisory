# Enable optional dependencies (required for platform-specific binaries)
include=optional

# Strict peer dependencies
legacy-peer-deps=false

# Ensure native binaries are installed for the target platform
# This fixes issues with lightningcss and rollup binaries in CI
